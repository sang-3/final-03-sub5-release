import LogoTitle from "@/app/components/landing/LogoTitle";
import StartButton from "@/app/components/landing/StartButton";

export default function LandingContainer() {
  return (
    <main className="min-h-screen flex flex-col">
      <LogoTitle />
    </main>
  );
}
