# 기능 정의서 - 러닝 기록 관리 웹앱

## 문서 정보

본 문서는 러닝 기록 관리 웹앱의 각 기능에 대한 입력, 처리, 출력 및 예외 사항을 상세히 정의합니다.

---

## 목차

1. [공통 규칙](#1-공통-규칙)
2. [홈/로그인/회원가입](#2-홈로그인회원가입)
3. [실시간 날씨 및 네비게이션](#3-실시간-날씨-및-네비게이션)
4. [유저 기록 관리](#4-유저-기록-관리)
5. [목표 설정 추천](#5-목표-설정-추천)
6. [마이페이지](#6-마이페이지)
7. [데이터 구조](#7-데이터-구조)
8. [예외 처리](#8-예외-처리)

---

## 1. 공통 규칙

### 1.1 입력 검증
- 필수 입력 항목이 누락된 경우 다음 단계로 진행할 수 없음
- 입력값 검증 실패 시 구체적인 오류 메시지 제공
- 실시간 검증 피드백 제공 (입력 중 유효성 확인)

### 1.2 저장 처리
- 모든 저장/수정/삭제 작업은 사용자 확인 후 진행
- 저장 성공 시 성공 메시지 표시 및 화면 전환
- 저장 실패 시 오류 메시지 표시 및 재시도 옵션 제공

### 1.3 데이터 표시
- 데이터가 없는 경우 "기록 없음" 또는 "데이터가 없습니다" 메시지 표시
- 로딩 중에는 스켈레톤 UI 또는 로딩 스피너 표시
- 에러 발생 시 에러 메시지와 재시도 버튼 제공

---

## 2. 홈/로그인/회원가입

### 2.1 로그인 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-L-01 | 일반 로그인 | 1순위 | 이메일, 비밀번호 | 계정 확인 → 비밀번호 검증 → JWT 토큰 발급 | 로그인 성공, 메인 홈 이동 | 계정 없음, 비밀번호 불일치 |
| FD-L-02 | 소셜 로그인 (구글) | 1순위 | 구글 계정 | OAuth 인증 → 사용자 정보 조회 → 계정 확인 | 로그인 성공 또는 추가 정보 입력 | OAuth 인증 실패 |
| FD-L-03 | 소셜 로그인 (카카오) | 1순위 | 카카오 계정 | OAuth 인증 → 사용자 정보 조회 → 계정 확인 | 로그인 성공 또는 추가 정보 입력 | OAuth 인증 실패 |

### 2.2 회원가입 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-S-01 | 일반 회원가입 | 1순위 | 이메일, 비밀번호, 닉네임, 성별, 생년월일, 키, 몸무게 | 중복 확인 → 비밀번호 암호화 → BMI 계산 → DB 저장 → 자동 로그인 | 회원가입 완료, 메인 홈 이동 | 이메일/닉네임 중복, 유효성 검증 실패 |
| FD-S-02 | 소셜 회원가입 | 1순위 | 소셜 계정 정보, 닉네임, 성별, 생년월일, 키, 몸무게 | 약관 동의 → 추가 정보 입력 → BMI 계산 → DB 저장 → 자동 로그인 | 회원가입 완료, 메인 홈 이동 | 약관 미동의, 유효성 검증 실패 |
| FD-S-03 | 이메일 중복 확인 | 1순위 | 이메일 | DB 조회 | 사용 가능/불가능 안내 | - |
| FD-S-04 | 닉네임 중복 확인 | 1순위 | 닉네임 | DB 조회 | 사용 가능/불가능 안내 | - |
| FD-S-05 | BMI 자동 계산 | 1순위 | 키, 몸무게 | BMI = 몸무게(kg) / (키(m))² | BMI 값 표시 | 키 또는 몸무게 미입력 |

### 2.3 메인 홈 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-H-01 | 메인 홈 진입 | 1순위 | 로그인 상태 | 사용자 정보 조회 | 동기부여 멘트, 목표/기록 버튼, 대회 정보 표시 | 로그인 안 됨 |
| FD-H-02 | 마라톤 대회 정보 조회 | 1순위 | 국내/해외 탭 선택 | 더미 JSON 데이터 필터링 → D-Day 계산 | 대회 카드 리스트 표시 | 데이터 없음 |

---

## 3. 실시간 날씨 및 네비게이션

### 3.1 날씨 위젯 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-W-01 | 로그인 상태 확인 | 1순위 | 사용자 세션/토큰 | 로그인 여부 확인 | 로그인 상태 (true/false) | - |
| FD-W-02 | 날씨 위젯 표시 조건 | 1순위 | 로그인 상태 | 로그인 = true면 위젯 표시, false면 완전 숨김 | 날씨 위젯 표시 또는 숨김 | 비로그인 시 API 호출 안 함 |
| FD-W-03 | 위치 권한 요청 | 1순위 | 날씨 위젯 첫 로드 시 | Geolocation API로 위치 권한 요청 | 권한 허용 시 좌표 획득, 거부 시 안내 메시지 | 권한 거부, 브라우저 미지원 |
| FD-W-04 | 현재 위치 조회 | 1순위 | 위치 권한 허용 | navigator.geolocation.getCurrentPosition() 호출 | 위도(latitude), 경도(longitude) | 위치 조회 실패 시 오류 메시지 |
| FD-W-05 | 기상청 API 호출 | 1순위 | 위도, 경도 | 기상청 API로 날씨 데이터 요청 | 온도, 습도, 날씨 상태, 풍속, 체감온도 | API 호출 실패, 타임아웃 |
| FD-W-06 | 러닝 적합도 계산 | 1순위 | 온도, 습도, 날씨 상태 | 조건 기반 적합도 판단 (최적/양호/보통/주의/부적합) | 적합도 등급 및 안내 문구 | - |
| FD-W-07 | 날씨 위젯 UI 표시 | 1순위 | 온도, 이모지, 안내 문구 | 헤더에 간단한 날씨 정보 표시 | "18°C ☀️ 러닝하기 좋은 날씨!" 형식 | 데이터 없을 시 "날씨 정보 없음" |
| FD-W-08 | 날씨 상태별 색상 변경 | 1순위 | 날씨 상태 | 날씨 상태에 따라 위젯 배경 색상 변경 | 변경된 배경 색상 | - |
| FD-W-09 | 날씨 상태별 이모지 표시 | 1순위 | 날씨 상태 | 날씨 상태에 따라 이모지 선택 | 날씨 이모지 (☀️, ☁️, 🌧️ 등) | - |

### 3.2 날씨 상세 화면 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-W-10 | 날씨 위젯 클릭 | 1순위 | 날씨 위젯 클릭 이벤트 | 날씨 상세 화면으로 이동 | 상세 화면 표시 | - |
| FD-W-11 | 동 단위 주소 조회 | 1순위 | 위도, 경도 | Reverse Geocoding API 호출 | 동 단위 주소 | Geocoding 실패 시 좌표만 표시 |
| FD-W-12 | 상세 날씨 정보 표시 | 1순위 | 날씨 데이터 | 온도, 습도, 날씨 상태, 풍속, 체감온도 표시 | 상세 날씨 정보 카드 | - |
| FD-W-13 | 러닝 적합도 상세 표시 | 1순위 | 적합도 등급, 안내 문구 | 적합도 등급 배지 + 상세 안내 문구 표시 | "☀️ 최적 - 러닝하기 완벽한 날씨입니다!" | - |
| FD-W-14 | 위치 재확인 버튼 | 1순위 | [위치 재확인] 버튼 클릭 | 현재 위치 재조회 → 날씨 데이터 갱신 | 갱신된 날씨 정보 표시 | 위치 권한 거부 시 "위치 재확인 불가" 안내 |

### 3.3 네비게이션 바 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-N-01 | 헤더 표시 조건 | 1순위 | 현재 화면 확인 | 홈 화면일 때만 헤더 고정 표시 | 헤더 표시 또는 숨김 | - |
| FD-N-02 | 로고 표시 및 동작 | 1순위 | - | 앱 로고 표시 | 로고 클릭 시 메인 홈으로 이동 | - |
| FD-N-03 | 유저 상태 드롭다운 | 1순위 | 유저 아이콘 클릭 | 드롭다운 메뉴 표시 | 내 프로필, 기록, 로그아웃 메뉴 | - |
| FD-N-04 | 하단 네비바 고정 표시 | 1순위 | - | 모든 화면 하단에 네비바 고정 | 네비바 표시 (홈, 기록, 목표, 프로필) | 비로그인 시 숨김 |
| FD-N-05 | 현재 페이지 하이라이트 | 1순위 | 현재 활성 페이지 | 현재 페이지의 아이콘/텍스트 강조 표시 | 활성 메뉴 하이라이트 | - |

### 3.4 러닝 적합도 계산 알고리즘

```javascript
function calculateRunningFitness(temperature, humidity, weatherStatus) {
  let grade, message, emoji, color;
  
  // 부적합: 비, 눈, 폭우 등
  if (['비', '눈', '폭우', '폭설', '뇌우'].includes(weatherStatus)) {
    grade = '부적합';
    message = '실내 운동을 권장합니다. 🏠';
    emoji = weatherStatus === '비' || weatherStatus === '폭우' ? '🌧️' : '❄️';
    color = '#A9A9A9';
  }
  // 주의: 극한 온도 또는 높은 습도
  else if (temperature > 30 || temperature < 0 || humidity > 80) {
    grade = '주의';
    message = '덥거나 습해요. 수분 보충 잊지 마세요! 💧';
    emoji = temperature > 30 ? '🥵' : '🥶';
    color = '#FFD700';
  }
  // 최적: 이상적인 조건
  else if (temperature >= 10 && temperature <= 20 && humidity >= 40 && humidity <= 60 
           && ['맑음', '구름 조금'].includes(weatherStatus)) {
    grade = '최적';
    message = '러닝하기 완벽한 날씨입니다! 🏃‍♂️';
    emoji = weatherStatus === '맑음' ? '☀️' : '⛅';
    color = '#87CEEB';
  }
  // 양호: 좋은 조건
  else if (temperature >= 5 && temperature <= 25 && humidity >= 30 && humidity <= 70
           && ['맑음', '구름 조금', '구름 많음', '흐림'].includes(weatherStatus)) {
    grade = '양호';
    message = '러닝하기 좋은 날씨예요! 😊';
    emoji = '☁️';
    color = '#B0C4DE';
  }
  // 보통: 그 외
  else {
    grade = '보통';
    message = '러닝 가능한 날씨입니다. 💪';
    emoji = '🌥️';
    color = '#D3D3D3';
  }
  
  return { grade, message, emoji, color };
}
```

---

## 4. 유저 기록 관리

### 4.1 대시보드 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-R-01 | 앵커 스크롤 네비게이션 | 1순위 | 네비게이션 버튼 클릭 | 해당 섹션 위치로 부드럽게 스크롤 이동 | 선택한 섹션 화면에 표시 | - |
| FD-R-02 | 오늘의 러닝 요약 조회 | 1순위 | 오늘 날짜 | 당일 기록 조회 및 총 거리/시간/평균페이스 계산 | 요약 카드 표시 | 데이터 없음 |
| FD-R-03 | 주간 러닝 거리 조회 | 1순위 | 현재 날짜 기준 1주 | 최근 7일간 거리 합산 | 주간 총 거리(km) 표시 | 데이터 없음 |
| FD-R-04 | 월간 러닝 거리 조회 | 1순위 | 현재 날짜 기준 1개월 | 최근 30일간 거리 합산 | 월간 총 거리(km) 표시 | 데이터 없음 |
| FD-R-05 | 최근 기록 조회 | 1순위 | - | 최신 5개 기록 조회 (최신순 정렬) | UI 카드 5개 표시 | 데이터 없음 |
| FD-R-06 | 주간 평균 페이스 통계 | 1순위 | 현재 날짜 기준 1주 | 일별 평균 페이스 계산 후 그래프 데이터 생성 | 꺾은선/막대 그래프 표시 | 데이터 없음 |

### 4.2 기록 추가 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-R-07 | 러닝 기록 추가 - 페이지 진입 | 1순위 | 추가 버튼 클릭 | 빈 입력 폼 표시 | 뒤로가기/저장/취소 버튼, 입력 필드 표시 | - |
| FD-R-08 | 러닝 기록 추가 - 데이터 입력 | 1순위 | 날짜, 운동시간, 거리, 운동유형 (필수), 운동장소, 소모칼로리, 메모 (선택) | 입력값 실시간 검증 | 유효성 피드백 표시 | 검증 실패 시 오류 메시지 |
| FD-R-09 | 평균 페이스 자동 계산 | 1순위 | 거리(km), 운동시간(시:분:초) | 시간을 초 단위로 변환 → (총 초 ÷ 거리) → "분:초/km" 형식 변환 | 페이스 필드에 자동 입력 | 거리 또는 시간 0일 때 계산 불가 안내 |
| FD-R-10 | 러닝 기록 저장 | 1순위 | 저장 버튼 클릭 | 필수 항목 확인 → DB 저장 | 저장 완료 메시지, 기록 관리 페이지로 이동 | 필수 항목 누락 시 저장 불가 안내 |

### 4.3 기록 수정/삭제 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-R-11 | 러닝 기록 수정 | 2순위 | 기록 카드에서 수정 버튼 클릭 | 기존 데이터 불러오기 → 수정 폼 표시 | 수정된 데이터 저장 후 목록으로 이동 | - |
| FD-R-12 | 러닝 기록 삭제 | 2순위 | 기록 카드에서 삭제 버튼 클릭 | 삭제 확인 모달 → 확인 시 DB 삭제 | 삭제 완료, 목록 갱신 | 취소 시 모달 닫기 |

### 4.4 부가 기능

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-R-13 | 운동 장소 선택 - 지도 | 2순위 | 지도 API 버튼 클릭 | 위치 권한 요청 → 지도 표시 → 핀 선택 | 선택한 위치 좌표 및 주소 저장 | 위치 권한 거부 시 직접 입력 안내 |
| FD-R-14 | 운동 장소 선택 - 직접 입력 | 2순위 | 텍스트 입력 | 입력값 저장 | 입력한 텍스트 저장 | - |
| FD-R-15 | 소모 칼로리 자동 계산 | 2순위 | 거리, 시간, 사용자 체중 (옵션) | 칼로리 = 거리(km) × 체중(kg) × 1.036 | 칼로리 필드에 자동 입력 | 수동 수정 가능 |
| FD-R-16 | 기록 필터링 | 2순위 | 기간 선택 (이번 주/월, 지난 주/월, 전체) | 선택된 기간에 해당하는 기록만 조회 | 필터링된 목록 표시 | 결과 없음 안내 |
| FD-R-17 | 기록 정렬 | 2순위 | 정렬 기준 선택 (최신순/거리순/페이스순) | 선택된 기준으로 목록 재정렬 | 정렬된 목록 표시 | - |
| FD-R-18 | 데이터 내보내기 | 3순위 | 내보내기 버튼 클릭, 형식 선택(CSV/JSON) | 모든 기록 데이터를 선택한 형식으로 변환 | 파일 다운로드 | 데이터 없을 시 안내 |
| FD-R-19 | 데이터 가져오기 | 3순위 | 파일 업로드 (CSV/JSON) | 파일 파싱 → 유효성 검증 → DB 저장 | 가져온 기록 수 안내, 목록 갱신 | 형식 오류 시 안내 |

---

## 5. 목표 설정 추천

### 5.1 레벨 분류 및 목표 추천

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-G-01 | 레벨 자동 분류 | 1순위 | 최근 한 달 러닝 기록 | 평균 페이스 계산 → 레벨 분류 (초급/중급/고급) | 레벨 배지 표시 | 기록 없음 시 초급 기본 설정 |
| FD-G-02 | 목표 추천 | 1순위 | 사용자 레벨 | 레벨에 맞는 목표 카드 생성 | 추천 목표 카드 리스트 표시 | - |
| FD-G-03 | 목표 상세 조회 | 1순위 | 목표 카드 선택 | 목표 상세 정보 조회 | 목표명, 거리/페이스, 달성 조건 표시 | - |
| FD-G-04 | 목표 설정 | 1순위 | [목표 설정] 버튼 클릭 | DB에 목표 정보 저장 → 미진행 상태로 설정 | 목표 진행 화면으로 이동 | 이미 진행 중인 목표 있을 시 안내 |

### 5.2 목표 진행 관리

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-G-05 | 러닝 시작 | 1순위 | [러닝 시작] 버튼 클릭 | 목표 상태를 "진행중"으로 변경 → 시작 일시 기록 | 진행중 화면으로 전환 | - |
| FD-G-06 | 진행률 계산 | 1순위 | 러닝 기록 추가 | (누적 거리 / 목표 거리) × 100 또는 페이스 조건 확인 | 진행률(%) 및 남은 거리 표시 | - |
| FD-G-07 | 완료 처리 | 1순위 | [완료 처리] 버튼 클릭 (조건 충족 시) | 목표 상태를 "완료"로 변경 → 달성 일시 기록 | 완료 화면으로 전환, 축하 메시지 | 조건 미충족 시 버튼 비활성화 |
| FD-G-08 | 다시 시작 | 1순위 | [다시 시작] 버튼 클릭 | 확인 모달 → 진행 데이터 초기화 | 진행중 상태 유지, 진행률 0으로 초기화 | 취소 시 모달 닫기 |
| FD-G-09 | 재도전 | 1순위 | [재도전] 버튼 클릭 | 확인 모달 → 목표 상태를 "미진행"으로 초기화 | 목표 추천 메인으로 이동 | 취소 시 모달 닫기 |

### 5.3 레벨별 목표 알고리즘

#### 레벨 분류 로직
```
1. 최근 한 달간 러닝 기록 조회
2. 각 기록의 페이스 계산
3. 평균 페이스 산출
4. 레벨 분류:
   - 평균 페이스 >= 5'30"/km → 초급
   - 4'30"/km <= 평균 페이스 < 5'30"/km → 중급
   - 평균 페이스 < 4'30"/km → 고급
5. 러닝 기록이 없는 경우 → 초급 (기본값)
```

#### 목표 진행률 계산
```
초급 (거리 기반):
  진행률 = (누적 거리 / 목표 거리) × 100

중급/고급 (페이스 챌린지):
  조건 충족 여부 = (최근 러닝 평균 페이스 <= 목표 페이스)

중급/고급 (거리 챌린지):
  진행률 = (누적 거리 / 목표 거리) × 100
  페이스 조건 = (평균 페이스 <= 목표 페이스)
```

---

## 6. 마이페이지

### 6.1 프로필 관리

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-P-01 | 프로필 조회 | 1순위 | 마이페이지 진입 | 사용자 정보 조회 (프로필 이미지, 닉네임) | 프로필 화면 표시 | - |
| FD-P-02 | 프로필 수정 | 1순위 | [수정] 버튼 클릭 → 이미지 업로드, 닉네임 입력 | 유효성 검증 → DB 저장 | 수정 완료, 프로필 화면으로 이동 | 닉네임 중복, 이미지 용량 초과 |

### 6.2 신체 정보 관리

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-P-03 | 러닝 지표 조회 | 1순위 | - | 러닝 레벨, 키, 몸무게, BMI 조회 | 러닝 지표 표시 | - |
| FD-P-04 | 러닝 레벨 자동 계산 | 1순위 | 평균 페이스 | 최근 한 달 평균 페이스 기반 레벨 계산 | 레벨 표시 (초급/중급/고급) | - |
| FD-P-05 | BMI 자동 계산 | 1순위 | 키, 몸무게 | BMI = 몸무게(kg) / (키(m))² | BMI 값 및 분류 표시 | - |
| FD-P-06 | 신체 정보 수정 | 1순위 | 각 필드별 수정 버튼 클릭 → 키, 몸무게 입력 | 유효성 검증 → DB 저장 → BMI 재계산 | 수정 완료, 러닝 지표 화면으로 이동 | 범위 초과 입력 |

### 6.3 게시판 관리

| 기능ID | 기능명 | 우선순위 | 입력 | 처리 | 출력 | 예외 |
|---|---|---|---|---|---|---|
| FD-P-07 | 내 게시글 조회 | 1순위 | - | 사용자가 작성한 게시글 목록 조회 | 게시글 목록 표시 (제목, 작성일, 답변 상태) | 게시글 없음 |
| FD-P-08 | 문의 게시판 조회 | 1순위 | 문의 게시판 버튼 클릭 | 전체 게시글 목록 조회 (공지글 상단 고정) | 게시글 목록 표시 | - |
| FD-P-09 | 게시글 작성 | 1순위 | [문의하기] 버튼 클릭 → 제목, 내용 입력 | 유효성 검증 → DB 저장 (답변 상태: 답변 대기) | 작성 완료, 게시판 목록으로 이동 | 필수 항목 누락 |
| FD-P-10 | 게시글 상세 조회 | 1순위 | 게시글 선택 (본인만) | 게시글 상세 정보 조회 | 제목, 내용, 댓글 표시 | 권한 없음 (타인 게시글) |
| FD-P-11 | 게시글 검색 | 1순위 | 검색어 입력 | 제목/내용에서 검색어 매칭 | 검색 결과 목록 표시 | 결과 없음 |
| FD-P-12 | 페이지네이션 | 1순위 | 페이지 번호 클릭 | 해당 페이지의 게시글 조회 | 페이지별 게시글 목록 표시 | - |
| FD-P-13 | 답변 상태 자동 변경 | 1순위 | 댓글 작성 (관리자) | 댓글 수 확인 → 답변 상태 변경 (답변 대기 → 답변 완료) | 답변 상태 업데이트 | - |

---

## 7. 데이터 구조

### 7.1 사용자 정보 (User)
```json
{
  "id": "고유 ID",
  "email": "이메일",
  "password": "암호화된 비밀번호 (소셜 로그인 시 null)",
  "nickname": "닉네임",
  "profileImage": "프로필 이미지 URL",
  "gender": "성별 (male/female)",
  "birthdate": "생년월일 (YYYY-MM-DD)",
  "height": "키 (cm)",
  "weight": "몸무게 (kg)",
  "bmi": "BMI 값",
  "provider": "로그인 방식 (email/google/kakao)",
  "createdAt": "가입 일시",
  "updatedAt": "수정 일시"
}
```

### 7.2 러닝 기록 (Running Record)
```json
{
  "id": "고유 ID",
  "userId": "사용자 ID",
  "date": "운동 날짜 (YYYY-MM-DD)",
  "duration": "운동 시간 (HH:MM:SS)",
  "distance": "거리 (km, 소수점 2자리)",
  "pace": "평균 페이스 (MM:SS/km)",
  "exerciseType": "운동 유형 (조깅, 인터벌, LSD 등)",
  "location": "운동 장소 (텍스트 또는 좌표)",
  "calories": "소모 칼로리 (kcal, 선택)",
  "memo": "메모 (선택)",
  "createdAt": "생성 일시",
  "updatedAt": "수정 일시"
}
```

### 7.3 목표 정보 (Goal)
```json
{
  "id": "고유 ID",
  "userId": "사용자 ID",
  "goalType": "목표 유형 (distance/pace/combined)",
  "targetDistance": "목표 거리 (km)",
  "targetPace": "목표 페이스 (MM:SS/km)",
  "currentDistance": "현재 누적 거리 (km)",
  "status": "진행 상태 (not_started/in_progress/completed)",
  "startDate": "시작 일시",
  "completedDate": "완료 일시",
  "progress": "진행률 (%)",
  "createdAt": "생성 일시",
  "updatedAt": "수정 일시"
}
```

### 7.4 날씨 정보 (Weather Data)
```json
{
  "location": {
    "latitude": "위도",
    "longitude": "경도",
    "address": "동 단위 주소"
  },
  "weather": {
    "temperature": "온도 (°C)",
    "humidity": "습도 (%)",
    "weatherStatus": "날씨 상태 (맑음, 구름, 비, 눈 등)",
    "windSpeed": "풍속 (m/s)",
    "feelsLike": "체감온도 (°C)"
  },
  "runningFitness": {
    "grade": "적합도 등급 (최적/양호/보통/주의/부적합)",
    "message": "안내 문구",
    "emoji": "이모지",
    "color": "배경 색상 코드"
  },
  "cachedAt": "캐시 시간",
  "expiresAt": "캐시 만료 시간"
}
```

### 7.5 게시글 (Post)
```json
{
  "id": "고유 ID",
  "userId": "작성자 ID",
  "title": "제목",
  "content": "내용",
  "isNotice": "공지글 여부 (true/false)",
  "status": "답변 상태 (pending/answered)",
  "commentCount": "댓글 수",
  "createdAt": "작성 일시",
  "updatedAt": "수정 일시"
}
```

---

## 8. 예외 처리

### 8.1 인증 관련

| 상황 | 처리 방식 |
|---|---|
| 비로그인 상태 접근 | 로그인 페이지로 리다이렉트 |
| 세션 만료 | "세션이 만료되었습니다" 메시지, 로그인 페이지로 이동 |
| 로그인 실패 | "이메일 또는 비밀번호가 일치하지 않습니다" 메시지 |
| 소셜 로그인 실패 | "소셜 로그인에 실패했습니다. 다시 시도해주세요" 메시지 |
| 권한 없음 | "접근 권한이 없습니다" 메시지 |

### 8.2 데이터 입력 관련

| 상황 | 처리 방식 |
|---|---|
| 필수 항목 누락 | 해당 필드 하이라이트 및 "필수 항목입니다" 메시지 |
| 이메일 형식 오류 | "올바른 이메일 형식을 입력해주세요" 메시지 |
| 비밀번호 규칙 위반 | "비밀번호는 8자 이상, 영문/숫자/특수문자를 포함해야 합니다" 메시지 |
| 이메일 중복 | "이미 사용 중인 이메일입니다" 메시지 |
| 닉네임 중복 | "이미 사용 중인 닉네임입니다" 메시지 |
| 범위 초과 입력 | "입력 범위를 확인해주세요 (예: 거리 0.1~200km)" 메시지 |
| 미래 날짜 입력 | "미래 날짜는 선택할 수 없습니다" 메시지 |

### 8.3 API 및 네트워크 관련

| 상황 | 처리 방식 |
|---|---|
| 네트워크 오류 | "네트워크 연결을 확인해주세요" 메시지 + [재시도] 버튼 |
| API 호출 실패 | "일시적인 오류가 발생했습니다. 다시 시도해주세요" 메시지 |
| API 타임아웃 | "요청 시간이 초과되었습니다. 다시 시도해주세요" 메시지 |
| 날씨 API 호출 실패 | "날씨 정보를 불러올 수 없습니다" 표시 + [재시도] 버튼 |
| 위치 권한 거부 | "위치 정보를 사용할 수 없습니다. 설정에서 권한을 허용해주세요" 안내 |
| Geolocation 미지원 | "브라우저가 위치 서비스를 지원하지 않습니다" 안내 |

### 8.4 데이터 없음

| 상황 | 처리 방식 |
|---|---|
| 러닝 기록 없음 | "첫 러닝을 기록해보세요!" 메시지 + [기록 추가] 버튼 |
| 검색 결과 없음 | "검색 결과가 없습니다" 메시지 |
| 게시글 없음 | "작성된 게시글이 없습니다" 메시지 |
| 목표 없음 | "목표를 설정해주세요" 메시지 + [목표 설정] 버튼 |

### 8.5 파일 업로드 관련

| 상황 | 처리 방식 |
|---|---|
| 파일 크기 초과 | "파일 크기는 5MB 이하만 가능합니다" 메시지 |
| 파일 형식 오류 | "지원하지 않는 파일 형식입니다 (jpg, png, gif만 가능)" 메시지 |
| 업로드 실패 | "파일 업로드에 실패했습니다. 다시 시도해주세요" 메시지 |
