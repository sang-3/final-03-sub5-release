# 시스템 설계서 - 러닝 기록 관리 웹앱

## 문서 정보

본 문서는 러닝 기록 관리 웹앱의 시스템 구성, 기술 스택, 데이터 흐름, 폴더 구조를 정의합니다. 개발팀 전체가 일관된 아키텍처 기준으로 작업할 수 있도록 가이드를 제공합니다.

---

## 목차

1. [기술 스택](#1-기술-스택)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [폴더 구조](#3-폴더-구조)
4. [데이터 흐름](#4-데이터-흐름)
5. [API 연동](#5-api-연동)
6. [상태 관리](#6-상태-관리)
7. [보안 및 인증](#7-보안-및-인증)
8. [성능 최적화](#8-성능-최적화)
9. [배포 전략](#9-배포-전략)

---

## 1. 기술 스택

### 1.1 Frontend

| 기술 | 버전 | 용도 |
|---|---|---|
| React | 19.2.3 | UI 라이브러리 |
| Next.js | 16.1.1 | React 프레임워크 (App Router) |
| JavaScript/TypeScript | ES2023 | 프로그래밍 언어 |
| Tailwind CSS | 4.x | CSS 프레임워크 |
| Chart.js / Recharts | Latest | 통계 그래프 |
| date-fns | Latest | 날짜 처리 |
| React Hook Form | Latest | 폼 관리 (선택) |

### 1.2 상태 관리

| 기술 | 용도 |
|---|---|
| Zustand | 전역 상태 관리 |
| sessionStorage | 인증 정보 영속화 |

### 1.3 서버 통신

| 기술 | 용도 |
|---|---|
| Fetch API | HTTP 요청 |
| Next.js Server Actions | 서버 사이드 로직 |
| JWT | 토큰 기반 인증 |

### 1.4 캐시 및 갱신

| 기술 | 용도 |
|---|---|
| fetch cache tags | 데이터 캐싱 |
| revalidatePath | 경로 기반 캐시 무효화 |
| updateTag | 태그 기반 캐시 무효화 |

### 1.5 개발 도구

| 도구 | 용도 |
|---|---|
| npm | 패키지 매니저 |
| ESLint 9 | 코드 품질 검사 |
| Prettier | 코드 포맷팅 |
| Husky | Git Hooks |
| lint-staged | Pre-commit 검사 |

### 1.6 배포

| 플랫폼 | 용도 |
|---|---|
| Vercel | 배포 플랫폼 |
| GitHub | 버전 관리 |

---

## 2. 시스템 아키텍처

### 2.1 전체 아키텍처

```
┌─────────────────────────────────────────┐
│         Client (Browser)                │
│  ┌───────────────────────────────────┐  │
│  │  React Components                 │  │
│  │  - Server Components (RSC)        │  │
│  │  - Client Components              │  │
│  │  - Server Actions                 │  │
│  └───────────────────────────────────┘  │
│              ↕                          │
│  ┌───────────────────────────────────┐  │
│  │  State Management (Zustand)       │  │
│  │  - User Store (sessionStorage)    │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                  ↕ HTTP/REST
┌─────────────────────────────────────────┐
│       Backend API Server                │
│  - 사용자 인증 (JWT)                    │
│  - 러닝 기록 CRUD                       │
│  - 목표 관리 CRUD                       │
│  - 게시판 CRUD                          │
│  - 데이터 저장 (DB)                     │
└─────────────────────────────────────────┘
                  ↕
┌─────────────────────────────────────────┐
│       External APIs                     │
│  - 기상청 API (날씨 정보)               │
│  - Geolocation API (위치)              │
│  - Reverse Geocoding API (주소)        │
│  - OAuth 2.0 (구글, 카카오)             │
└─────────────────────────────────────────┘
```

### 2.2 컴포넌트 구조

**Server Components (RSC)**
- 데이터 페칭 (목록/상세 조회)
- SEO 최적화 (메타데이터)
- 캐시 관리
- 초기 렌더링 성능 최적화

**Client Components**
- 폼 입력 및 유효성 검증
- 사용자 인터랙션 (클릭, 입력 등)
- 상태 관리 (Zustand 연동)
- 애니메이션 및 동적 UI

**Server Actions**
- 폼 제출 처리
- API 호출 및 에러 핸들링
- 캐시 갱신 (revalidatePath, updateTag)
- 리다이렉트 처리

---

## 3. 폴더 구조

```
📦 러닝기록관리웹앱
├── 📁 action/
│   ├── 📄 auth.ts              # 인증 관련 Server Actions
│   ├── 📄 record.ts            # 러닝 기록 CRUD Server Actions
│   ├── 📄 goal.ts              # 목표 관리 Server Actions
│   └── 📄 board.ts             # 게시판 Server Actions
│
├── 📁 app/
│   ├── 📁 (auth)/              # 라우트 그룹 (URL에 영향 없음)
│   │   ├── 📁 login/
│   │   │   ├── 📄 LoginForm.tsx       # 로그인 폼 (Client)
│   │   │   └── 📄 page.tsx            # 로그인 페이지
│   │   └── 📁 signup/
│   │       ├── 📄 SignupForm.tsx      # 회원가입 폼 (Client)
│   │       └── 📄 page.tsx            # 회원가입 페이지
│   │
│   ├── 📁 records/             # 러닝 기록 관리
│   │   ├── 📁 [id]/
│   │   │   ├── 📁 edit/
│   │   │   │   ├── 📄 EditRecordForm.tsx
│   │   │   │   └── 📄 page.tsx        # 기록 수정
│   │   │   └── 📄 page.tsx            # 기록 상세
│   │   │
│   │   ├── 📁 new/
│   │   │   ├── 📄 AddRecordForm.tsx   # 기록 추가 폼
│   │   │   └── 📄 page.tsx            # 기록 추가 페이지
│   │   │
│   │   ├── 📄 RecordItem.tsx          # 기록 카드 컴포넌트
│   │   ├── 📄 RecordChart.tsx         # 차트 컴포넌트
│   │   ├── 📄 RecordStats.tsx         # 통계 컴포넌트
│   │   ├── 📄 RecordFilter.tsx        # 필터 컴포넌트
│   │   └── 📄 page.tsx                # 메인 대시보드
│   │
│   ├── 📁 goals/               # 목표 설정
│   │   ├── 📁 [id]/
│   │   │   └── 📄 page.tsx            # 목표 상세
│   │   ├── 📁 progress/
│   │   │   └── 📄 page.tsx            # 목표 진행
│   │   ├── 📄 GoalCard.tsx            # 목표 카드 컴포넌트
│   │   ├── 📄 LevelBadge.tsx          # 레벨 배지
│   │   └── 📄 page.tsx                # 목표 추천 메인
│   │
│   ├── 📁 weather/             # 날씨 정보
│   │   ├── 📄 WeatherWidget.tsx       # 날씨 위젯 (Client)
│   │   └── 📄 page.tsx                # 날씨 상세
│   │
│   ├── 📁 profile/             # 마이페이지
│   │   ├── 📁 edit/
│   │   │   ├── 📄 ProfileEditForm.tsx
│   │   │   └── 📄 page.tsx            # 프로필 수정
│   │   ├── 📁 body-info/
│   │   │   ├── 📄 BodyInfoForm.tsx
│   │   │   └── 📄 page.tsx            # 신체 정보 수정
│   │   └── 📄 page.tsx                # 마이페이지 메인
│   │
│   ├── 📁 board/               # 문의 게시판
│   │   ├── 📁 [id]/
│   │   │   └── 📄 page.tsx            # 게시글 상세
│   │   ├── 📁 new/
│   │   │   ├── 📄 BoardForm.tsx
│   │   │   └── 📄 page.tsx            # 게시글 작성
│   │   ├── 📄 BoardItem.tsx           # 게시글 카드
│   │   └── 📄 page.tsx                # 게시판 목록
│   │
│   ├── 📄 error.tsx            # 전역 에러 페이지
│   ├── 📄 globals.css          # 전역 스타일
│   ├── 📄 layout.tsx           # 루트 레이아웃
│   ├── 📄 loading.tsx          # 전역 로딩 페이지
│   ├── 📄 not-found.tsx        # 404 페이지
│   └── 📄 page.tsx             # 홈 페이지
│
├── 📁 components/
│   └── 📁 common/
│       ├── 📁 ui/
│       │   ├── 📄 Button.tsx
│       │   ├── 📄 Input.tsx
│       │   ├── 📄 Modal.tsx
│       │   ├── 📄 Toast.tsx
│       │   └── 📄 LoadingSpinner.tsx
│       ├── 📄 Header.tsx               # 상단 헤더
│       ├── 📄 BottomNav.tsx            # 하단 네비게이션
│       ├── 📄 Footer.tsx
│       └── 📄 ErrorBoundary.tsx
│
├── 📁 lib/
│   ├── 📄 api.ts               # API 호출 공통 함수
│   ├── 📄 auth.ts              # 인증 관련 함수
│   ├── 📄 record.ts            # 러닝 기록 API
│   ├── 📄 goal.ts              # 목표 API
│   ├── 📄 weather.ts           # 날씨 API
│   ├── 📄 board.ts             # 게시판 API
│   └── 📄 file.ts              # 파일 업로드 함수
│
├── 📁 utils/
│   ├── 📄 pace.ts              # 페이스 계산 함수
│   ├── 📄 bmi.ts               # BMI 계산 함수
│   ├── 📄 date.ts              # 날짜 포맷 함수
│   ├── 📄 validation.ts        # 유효성 검증 함수
│   └── 📄 constants.ts         # 상수 정의
│
├── 📁 types/
│   ├── 📄 index.ts             # 타입 export
│   ├── 📄 api.ts               # API 응답 타입
│   ├── 📄 user.ts              # 사용자 타입
│   ├── 📄 record.ts            # 러닝 기록 타입
│   ├── 📄 goal.ts              # 목표 타입
│   ├── 📄 weather.ts           # 날씨 타입
│   └── 📄 board.ts             # 게시판 타입
│
├── 📁 zustand/
│   ├── 📄 userStore.ts         # 사용자 전역 상태
│   └── 📄 weatherStore.ts      # 날씨 전역 상태 (선택)
│
├── 📁 hooks/
│   ├── 📄 useAuth.ts           # 인증 커스텀 훅
│   ├── 📄 useRecord.ts         # 기록 관리 훅
│   ├── 📄 useGoal.ts           # 목표 관리 훅
│   └── 📄 useWeather.ts        # 날씨 정보 훅
│
├── 📁 public/
│   └── 📁 images/
│       ├── favicon.svg
│       ├── logo.png
│       └── placeholder.png
│
├── 📄 .env.local               # 환경 변수
├── 📄 .gitignore
├── 📄 eslint.config.mjs
├── 📄 next.config.ts
├── 📄 package.json
├── 📄 postcss.config.mjs
├── 📄 README.md
├── 📄 tailwind.config.ts
└── 📄 tsconfig.json
```

---

## 4. 데이터 흐름

### 4.1 기록 조회 (Read)

```
1. 사용자가 페이지 진입 (/records)
   ↓
2. Server Component에서 데이터 페칭
   - lib/record.ts의 getRecords() 호출
   - fetch with cache tags: ["records"]
   ↓
3. Backend API 요청
   - GET /api/records
   - Authorization: Bearer {accessToken}
   - Client-Id: {clientId}
   ↓
4. 서버 응답 (RecordListRes)
   {
     ok: 1,
     item: [RunningRecord]
   }
   ↓
5. 컴포넌트 렌더링
   - RecordItem 컴포넌트로 목록 표시
   - 캐시 저장 (next: { tags: ["records"] })
```

### 4.2 기록 추가 (Create)

```
1. 사용자가 폼 입력 (/records/new)
   - Client Component (AddRecordForm)
   - useActionState로 Server Action 연결
   ↓
2. 저장 버튼 클릭
   - useActionState로 createRecord Server Action 호출
   - FormData 전달
   ↓
3. Server Action (action/record.ts의 createRecord)
   - FormData 파싱
   - 유효성 검증
   - accessToken 가져오기 (Zustand store)
   - API 요청 (POST /api/records)
   ↓
4. Backend API 응답
   {
     ok: 1,
     item: RunningRecord
   }
   ↓
5. 성공 시:
   - updateTag("records")로 캐시 무효화
   - redirect("/records")로 목록 페이지 이동
   실패 시:
   - 에러 상태 반환
   - 클라이언트에서 에러 메시지 표시
```

### 4.3 로그인 흐름

```
1. 로그인 폼 제출 (Client Component)
   - useActionState로 login Server Action 호출
   ↓
2. Server Action (action/auth.ts의 login)
   - FormData에서 email, password 추출
   - POST /api/users/login
   ↓
3. 성공 시 서버 응답
   {
     ok: 1,
     item: {
       accessToken: "...",
       refreshToken: "...",
       user: { ... }
     }
   }
   ↓
4. Client Component (LoginForm)
   - useEffect에서 state 확인
   - Zustand의 setUser(user, accessToken) 호출
   - sessionStorage에 자동 저장
   - redirect("/")로 메인 홈 이동
```

### 4.4 날씨 정보 조회

```
1. 사용자 로그인 확인
   ↓
2. 로그인 시 날씨 위젯 표시 (Client Component)
   - useEffect에서 위치 권한 요청
   - navigator.geolocation.getCurrentPosition()
   ↓
3. 위도, 경도 획득
   ↓
4. 기상청 API 호출
   - GET /weather?lat={lat}&lon={lon}
   ↓
5. 날씨 데이터 수신
   {
     temperature: 18,
     humidity: 55,
     weatherStatus: "맑음",
     windSpeed: 2.5,
     feelsLike: 17
   }
   ↓
6. 러닝 적합도 계산
   - calculateRunningFitness(temperature, humidity, weatherStatus)
   ↓
7. 날씨 위젯 UI 렌더링
   - 온도 + 이모지 + 배경 색상 + 안내 문구
   ↓
8. 캐싱 (10분)
   - localStorage에 캐시 저장
   - expiresAt 시간까지 유효
```

### 4.5 목표 추천 및 진행

```
1. 목표 추천 메인 진입 (/goals)
   ↓
2. Server Component에서 레벨 분류
   - 최근 한 달 러닝 기록 조회
   - 평균 페이스 계산
   - 레벨 분류 (초급/중급/고급)
   ↓
3. 레벨별 목표 카드 표시
   ↓
4. 목표 선택 → 목표 상세 → [목표 설정] 클릭
   ↓
5. Server Action (createGoal)
   - DB에 목표 저장
   - status: "not_started"
   - updateTag("goals")
   - redirect("/goals/progress")
   ↓
6. 목표 진행 화면
   - 미진행 → [러닝 시작] → 진행중
   - 러닝 기록 추가 시 진행률 자동 계산
   - 조건 충족 시 [완료 처리] 활성화
   - 완료 → 축하 메시지 + [재도전]
```

---

## 5. API 연동

### 5.1 공통 설정

#### 환경 변수
```bash
# .env.local
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_CLIENT_ID=your-client-id
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret
```

#### 공통 헤더
```typescript
const headers = {
  "Content-Type": "application/json",
  "Client-Id": process.env.NEXT_PUBLIC_CLIENT_ID,
  "Authorization": `Bearer ${accessToken}` // 인증 필요 시
};
```

### 5.2 주요 엔드포인트

#### 인증 API

| Method | Endpoint | 설명 | 인증 필요 |
|---|---|---|---|
| POST | `/users/login` | 일반 로그인 | ❌ |
| POST | `/users` | 회원가입 | ❌ |
| POST | `/users/oauth/google` | 구글 소셜 로그인 | ❌ |
| POST | `/users/oauth/kakao` | 카카오 소셜 로그인 | ❌ |

#### 러닝 기록 API

| Method | Endpoint | 설명 | 인증 필요 |
|---|---|---|---|
| GET | `/records` | 기록 목록 조회 | ✅ |
| GET | `/records/:id` | 기록 상세 조회 | ✅ |
| POST | `/records` | 기록 추가 | ✅ |
| PUT | `/records/:id` | 기록 수정 | ✅ |
| DELETE | `/records/:id` | 기록 삭제 | ✅ |

#### 목표 API

| Method | Endpoint | 설명 | 인증 필요 |
|---|---|---|---|
| GET | `/goals` | 목표 목록 조회 | ✅ |
| GET | `/goals/:id` | 목표 상세 조회 | ✅ |
| POST | `/goals` | 목표 설정 | ✅ |
| PUT | `/goals/:id` | 목표 수정 | ✅ |
| DELETE | `/goals/:id` | 목표 삭제 | ✅ |

#### 게시판 API

| Method | Endpoint | 설명 | 인증 필요 |
|---|---|---|---|
| GET | `/board` | 게시글 목록 조회 | ✅ |
| GET | `/board/:id` | 게시글 상세 조회 | ✅ |
| POST | `/board` | 게시글 작성 | ✅ |
| PUT | `/board/:id` | 게시글 수정 | ✅ |
| DELETE | `/board/:id` | 게시글 삭제 | ✅ |
| POST | `/board/:id/comments` | 댓글 작성 (관리자) | ✅ |

#### 외부 API

| API | Endpoint | 설명 |
|---|---|---|
| 기상청 API | `https://api.weather.go.kr/...` | 날씨 정보 조회 |
| Reverse Geocoding | `https://geocoding.api/...` | 좌표 → 주소 변환 |

### 5.3 API 응답 구조

#### 성공 응답
```typescript
// 목록 조회
{
  ok: 1,
  item: RunningRecord[] | Goal[] | Post[]
}

// 상세 조회/생성/수정
{
  ok: 1,
  item: RunningRecord | Goal | Post
}

// 삭제
{
  ok: 1
}
```

#### 에러 응답
```typescript
{
  ok: 0,
  message: string,
  errors?: {
    [fieldName: string]: {
      msg: string;
    }
  }
}
```

### 5.4 캐시 전략

#### 캐시 태그 사용
```typescript
// lib/record.ts
export async function getRecords() {
  const res = await fetch(`${API_URL}/records`, {
    headers: {
      "Client-Id": CLIENT_ID,
      "Authorization": `Bearer ${accessToken}`
    },
    cache: "force-cache",
    next: {
      tags: ["records"]  // 캐시 태그
    }
  });
  return res.json();
}
```

#### 캐시 무효화
```typescript
// action/record.ts
export async function createRecord(formData: FormData) {
  // ... API 호출 ...
  
  if (data.ok) {
    updateTag("records");  // 캐시 무효화
    redirect("/records");
  }
}
```

---

## 6. 상태 관리

### 6.1 Zustand Store

#### 사용자 상태 관리
```typescript
// zustand/userStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface User {
  id: string;
  email: string;
  nickname: string;
  profileImage?: string;
  // ... 기타 사용자 정보
}

interface UserStoreState {
  user: User | null;
  accessToken: string | null;
  setUser: (user: User, accessToken: string) => void;
  resetUser: () => void;
}

export const useUserStore = create<UserStoreState>()(
  persist(
    (set) => ({
      user: null,
      accessToken: null,
      setUser: (user, accessToken) => set({ user, accessToken }),
      resetUser: () => set({ user: null, accessToken: null }),
    }),
    {
      name: 'user-storage',
      storage: {
        getItem: (name) => {
          const str = sessionStorage.getItem(name);
          return str ? JSON.parse(str) : null;
        },
        setItem: (name, value) => {
          sessionStorage.setItem(name, JSON.stringify(value));
        },
        removeItem: (name) => sessionStorage.removeItem(name),
      },
    }
  )
);
```

### 6.2 Client Component에서 상태 사용

```typescript
// components/Header.tsx
'use client';

import { useUserStore } from '@/zustand/userStore';

export default function Header() {
  const { user, resetUser } = useUserStore();

  const handleLogout = () => {
    resetUser();
    // 로그아웃 처리
  };

  return (
    <header>
      {user ? (
        <>
          <span>{user.nickname}</span>
          <button onClick={handleLogout}>로그아웃</button>
        </>
      ) : (
        <a href="/login">로그인</a>
      )}
    </header>
  );
}
```

### 6.3 Server Action에서 상태 접근

Server Action은 서버에서 실행되므로 Zustand store에 직접 접근할 수 없습니다. 대신 클라이언트에서 accessToken을 전달하거나, 쿠키를 사용합니다.

```typescript
// action/record.ts
'use server';

import { cookies } from 'next/headers';

export async function createRecord(formData: FormData) {
  // 쿠키에서 토큰 가져오기 (대안)
  const cookieStore = cookies();
  const token = cookieStore.get('accessToken')?.value;

  // 또는 FormData에 토큰 포함
  const token = formData.get('accessToken') as string;

  const response = await fetch(`${API_URL}/records`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      // ... 데이터
    })
  });

  const data = await response.json();

  if (data.ok) {
    revalidatePath('/records');
    redirect('/records');
  }

  return data;
}
```

---

## 7. 보안 및 인증

### 7.1 JWT 토큰 관리

#### 토큰 저장
- **accessToken**: sessionStorage (브라우저 종료 시 삭제)
- **refreshToken**: httpOnly 쿠키 (선택, 서버에서 관리)

#### 토큰 갱신
```typescript
// lib/auth.ts
export async function refreshAccessToken(refreshToken: string) {
  const response = await fetch(`${API_URL}/users/refresh`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ refreshToken })
  });

  const data = await response.json();

  if (data.ok) {
    return data.item.accessToken;
  }

  throw new Error('Token refresh failed');
}
```

### 7.2 인증 미들웨어

```typescript
// middleware.ts
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  const token = request.cookies.get('accessToken')?.value;

  // 인증이 필요한 페이지
  const protectedPaths = ['/records', '/goals', '/profile'];
  const isProtectedPath = protectedPaths.some(path =>
    request.nextUrl.pathname.startsWith(path)
  );

  if (isProtectedPath && !token) {
    return NextResponse.redirect(new URL('/login', request.url));
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/records/:path*', '/goals/:path*', '/profile/:path*'],
};
```

### 7.3 권한 검증

#### 클라이언트 사이드
```typescript
// hooks/useAuth.ts
import { useUserStore } from '@/zustand/userStore';
import { useRouter } from 'next/navigation';
import { useEffect } from 'react';

export function useAuth() {
  const { user, accessToken } = useUserStore();
  const router = useRouter();

  useEffect(() => {
    if (!user || !accessToken) {
      router.push('/login');
    }
  }, [user, accessToken, router]);

  return { user, accessToken };
}
```

#### 서버 사이드
```typescript
// action/record.ts
export async function deleteRecord(id: string) {
  const token = await getAccessToken(); // 쿠키 또는 다른 방법

  if (!token) {
    throw new Error('Unauthorized');
  }

  const response = await fetch(`${API_URL}/records/${id}`, {
    method: 'DELETE',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });

  // ... 처리
}
```

### 7.4 XSS 및 CSRF 방어

- **XSS**: React의 기본 이스케이핑 활용, dangerouslySetInnerHTML 사용 금지
- **CSRF**: SameSite 쿠키 설정, CSRF 토큰 사용 (선택)

---

## 8. 성능 최적화

### 8.1 캐싱 전략

#### 데이터 캐싱
```typescript
// 정적 데이터 (마라톤 대회 정보)
export async function getMarathonEvents() {
  const res = await fetch(`${API_URL}/marathons`, {
    cache: 'force-cache',
    next: {
      tags: ['marathons'],
      revalidate: 3600 // 1시간
    }
  });
  return res.json();
}

// 동적 데이터 (러닝 기록)
export async function getRecords() {
  const res = await fetch(`${API_URL}/records`, {
    cache: 'no-store', // 또는 적절한 캐시 전략
    next: {
      tags: ['records']
    }
  });
  return res.json();
}
```

#### 날씨 데이터 캐싱
```typescript
// utils/weather.ts
const WEATHER_CACHE_DURATION = 10 * 60 * 1000; // 10분

export function getCachedWeather(lat: number, lon: number) {
  const cacheKey = `weather_${lat}_${lon}`;
  const cached = localStorage.getItem(cacheKey);

  if (cached) {
    const data = JSON.parse(cached);
    if (Date.now() < data.expiresAt) {
      return data;
    }
  }

  return null;
}

export function setCachedWeather(lat: number, lon: number, data: any) {
  const cacheKey = `weather_${lat}_${lon}`;
  const cachedData = {
    ...data,
    cachedAt: Date.now(),
    expiresAt: Date.now() + WEATHER_CACHE_DURATION
  };
  localStorage.setItem(cacheKey, JSON.stringify(cachedData));
}
```

### 8.2 이미지 최적화

```tsx
import Image from 'next/image';

<Image
  src="/images/logo.png"
  alt="Logo"
  width={200}
  height={50}
  priority // 중요 이미지
/>

<Image
  src={profileImage}
  alt="Profile"
  width={100}
  height={100}
  loading="lazy" // 지연 로딩
/>
```

### 8.3 코드 분할

```tsx
import dynamic from 'next/dynamic';

// 차트 라이브러리 동적 임포트
const RecordChart = dynamic(() => import('./RecordChart'), {
  loading: () => <p>Loading chart...</p>,
  ssr: false // 클라이언트에서만 렌더링
});
```

### 8.4 번들 최적화

```javascript
// next.config.ts
const nextConfig = {
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production', // 프로덕션에서 console 제거
  },
  images: {
    domains: ['api.example.com'], // 외부 이미지 도메인
  },
};

export default nextConfig;
```

---

## 9. 배포 전략

### 9.1 Vercel 배포

#### 자동 배포
```bash
# main 브랜치 push 시 자동 배포
git push origin main
```

#### 환경 변수 설정
Vercel 대시보드에서 환경 변수 설정:
- `NEXT_PUBLIC_API_URL`
- `NEXT_PUBLIC_CLIENT_ID`
- `GOOGLE_CLIENT_ID` (비밀)
- `GOOGLE_CLIENT_SECRET` (비밀)
- `KAKAO_CLIENT_ID` (비밀)
- `KAKAO_CLIENT_SECRET` (비밀)

### 9.2 빌드 최적화

```bash
# 프로덕션 빌드
npm run build

# 빌드 분석
npm run build -- --analyze

# 로컬에서 프로덕션 모드 실행
npm run start
```

### 9.3 모니터링

#### Vercel Analytics (선택)
```tsx
// app/layout.tsx
import { Analytics } from '@vercel/analytics/react';

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
```

#### 에러 추적 (Sentry, 선택)
```tsx
// sentry.client.config.ts
import * as Sentry from '@sentry/nextjs';

Sentry.init({
  dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
  tracesSampleRate: 1.0,
});
```

### 9.4 CI/CD 파이프라인

```yaml
# .github/workflows/deploy.yml
name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
      - name: Build
        run: npm run build
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
```

---

## 10. 개발 환경 설정

### 10.1 필수 설치

```bash
# Node.js 20 이상 필요
node -v

# 프로젝트 초기화
npm install

# 개발 서버 실행
npm run dev
```

### 10.2 환경 변수 설정

```bash
# .env.local 생성
cp .env.example .env.local

# 환경 변수 입력
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_CLIENT_ID=your-client-id
# ... 기타 환경 변수
```

### 10.3 코드 품질 도구

```bash
# ESLint 실행
npm run lint

# Prettier 실행
npm run format

# 타입 체크
npm run type-check
```

---

## 11. 팀 협업 규칙

### 11.1 Git 브랜치 전략

```
main (프로덕션)
  ↓
develop (개발)
  ↓
feature/기능명 (기능 개발)
  ↓
hotfix/버그명 (긴급 수정)
```

### 11.2 커밋 컨벤션

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅 (기능 변경 없음)
refactor: 리팩토링
test: 테스트 추가
chore: 빌드, 설정 변경
```

### 11.3 Pull Request 규칙

- 최소 1명 리뷰 + Approve
- CI 테스트 통과 확인
- Squash and Merge 사용

---

## 12. 유지보수 계획

### 12.1 로깅

```typescript
// utils/logger.ts
export const logger = {
  info: (message: string, data?: any) => {
    console.log(`[INFO] ${message}`, data);
  },
  error: (message: string, error?: any) => {
    console.error(`[ERROR] ${message}`, error);
    // Sentry에 전송 (선택)
  },
  warn: (message: string, data?: any) => {
    console.warn(`[WARN] ${message}`, data);
  },
};
```

### 12.2 업데이트 전략

- **주요 라이브러리**: 분기별 업데이트 검토
- **보안 패치**: 즉시 적용
- **의존성 관리**: npm audit 정기 실행

```bash
# 보안 취약점 검사
npm audit

# 자동 수정
npm audit fix
```

### 12.3 성능 모니터링

```typescript
// utils/performance.ts
export function measurePerformance(name: string) {
  const start = performance.now();

  return {
    end: () => {
      const duration = performance.now() - start;
      console.log(`[Performance] ${name}: ${duration.toFixed(2)}ms`);
    },
  };
}
```

---

## 부록

### A. 주요 계산 공식

#### 페이스 계산
```typescript
// utils/pace.ts
export function calculatePace(distance: number, timeInSeconds: number): string {
  if (distance <= 0 || timeInSeconds <= 0) return '0:00/km';

  const paceInSeconds = timeInSeconds / distance;
  const minutes = Math.floor(paceInSeconds / 60);
  const seconds = Math.floor(paceInSeconds % 60);

  return `${minutes}:${seconds.toString().padStart(2, '0')}/km`;
}
```

#### BMI 계산
```typescript
// utils/bmi.ts
export function calculateBMI(weight: number, height: number): number {
  if (weight <= 0 || height <= 0) return 0;

  const heightInMeters = height / 100;
  return Number((weight / (heightInMeters ** 2)).toFixed(1));
}

export function getBMICategory(bmi: number): string {
  if (bmi < 18.5) return '저체중';
  if (bmi < 23.0) return '정상';
  if (bmi < 25.0) return '과체중';
  if (bmi < 30.0) return '비만';
  return '고도 비만';
}
```

#### 칼로리 계산
```typescript
// utils/calorie.ts
export function calculateCalories(distance: number, weight: number = 70): number {
  // 칼로리 = 거리(km) × 체중(kg) × 1.036
  return Math.round(distance * weight * 1.036);
}
```

### B. 유용한 유틸리티 함수

```typescript
// utils/date.ts
import { format, formatDistanceToNow } from 'date-fns';
import { ko } from 'date-fns/locale';

export function formatDate(date: string | Date, formatStr: string = 'yyyy-MM-dd'): string {
  return format(new Date(date), formatStr, { locale: ko });
}

export function getRelativeTime(date: string | Date): string {
  return formatDistanceToNow(new Date(date), { addSuffix: true, locale: ko });
}
```

```typescript
// utils/validation.ts
export function isValidEmail(email: string): boolean {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
}

export function isValidPassword(password: string): boolean {
  // 최소 8자, 영문/숫자/특수문자 각 1개 이상
  const passwordRegex = /^(?=.*[A-Za-z])(?=.*\d)(?=.*[@$!%*#?&])[A-Za-z\d@$!%*#?&]{8,}$/;
  return passwordRegex.test(password);
}

export function isValidNickname(nickname: string): boolean {
  // 2~10자
  return nickname.length >= 2 && nickname.length <= 10;
}
```
